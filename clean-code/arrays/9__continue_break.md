### continue


```
const orders = ['first', 'second', 'thrid']

for(let index = 0 ; index< array.length; index++) {
  const order = orders[index]
  
  if(order === 'second') {
    continue;
    break;
  }
}

orders.forEach(function(order) {
  if(order === 'second') {
    // ~~
    
  }
})

```

